# Copy this to .env and fill in your values.
# Never commit .env — it contains RPC credentials.

RPC_URL=http://127.0.0.1:18443
RPC_USER=user
RPC_PASS=pass

# Optional — defaults shown below
# WALLET_DB=wallet.db
# DESCRIPTOR_FILE=wallet_descriptors.json

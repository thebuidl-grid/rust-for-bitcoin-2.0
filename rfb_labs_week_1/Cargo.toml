[package]
name = "rfb-labs-week-1"
version = "0.1.0"
edition = "2021"
description = "Rust for Bitcoin 2.0 — Week 1 practical Bitcoin fundamentals labs"
license = "MIT"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
